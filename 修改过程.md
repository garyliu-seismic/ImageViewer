# 修改过程记录

本文件记录近期针对「视频播放卡死」问题的排查与修复过程。

## 问题一：播放视频中打开另一个视频卡死

### 现象

正在播放视频时，打开另一个视频文件（或切换到下一个视频），程序卡住无响应。

### 定位

用 py-spy 挂上卡死的进程，native 栈显示主线程卡在：

```
libvlc_media_player_stop
  → input_resource_Terminate
  → vout_Close
  → vlc_join          # 等视频输出（vout）线程退出
```

主线程阻塞在 `player.stop()` 里等 vout 线程退出，而 vout 线程又在等主线程去处理
窗口消息（主线程此刻卡在 `stop()` 里没机会泵消息）→ 死锁。

### 深层根因

翻 VLC 3.0.20 源码发现 `libvlc_media_player_set_hwnd()` 会把播放器上的
`avcodec-hw` 和 `vout` 两个变量重置为空字符串：

```c
var_SetString(p_mi, "avcodec-hw", "");
var_SetString(p_mi, "vout", "");
```

所以之前尝试用 `--avcodec-hw=none`、`--vout=wingdi`（Instance 选项）关掉硬件
解码/渲染，一调 `set_hwnd()` 就被抹掉了，D3D11VA 硬件解码 + direct3d11 渲染
其实一直在跑，死锁依旧。

### 修复

1. **硬件解码改软件**：`--avcodec-hw=none` 作为 Instance 选项在 VLC 3.0.20 里
   不生效，必须作为 **media 选项** 传：
   ```python
   media = self.vlc_instance.media_new(path, ":avcodec-hw=none")
   ```
   实测这是唯一能扛过 `set_hwnd()`、真正关掉 D3D11VA 硬件解码的方式。

2. **`player.stop()` 改后台线程 + 主线程泵事件**：`stop()` 放到后台线程执行，
   主线程在等待期间持续 `self.update()` 泵 Tk 事件，让 vout 线程能正常收尾，
   破解 `vout_Close` 死锁（加 8s 超时兜底）。

## 问题二：播放中加载字幕卡死

### 现象

问题一修复后，打开新视频正常了，但「播放当中开启字幕」仍会卡住。

### 定位

py-spy native 栈显示主线程卡在：

```
libvlc_media_player_get_length   # 等一把 libvlc 内部临界区锁
  ← _update_video_time_loop（500ms 进度条定时器）
  ← _wait_stop 里的 self.update() 泵事件
  ← _stop_player ← _finish_start（字幕加载路径）

另一个线程：player.stop()        # 握着那把锁
```

即：字幕引擎在绑定音频回调前要停掉正在播放的视频，`stop()` 在后台线程跑、
主线程 `self.update()` 泵事件；泵事件期间 **500ms 的进度条定时器照常触发，
去调 `get_length()`**，撞上 `stop()` 持有的 libvlc 内部锁，主线程反而卡死在
`get_length()` 里 → 又死锁。

### 修复

1. **字幕引擎注入安全停函数**：`caption_engine.start_async(player, stop_fn=...)`
   新增 `stop_fn` 参数；`_finish_start` 里需要停 player 时改调 `stop_fn(player)`
   （App 传入自己的后台线程 stop + pump 实现），不再在主线程同步 `player.stop()`。

2. **加 `_video_is_stopping()` 护栏**：`_stop_event` 未完成即视为正在停，所有会
   在停播期间碰 player 的回调直接返回：
   - `_update_video_time_loop`（进度条定时器，元凶）
   - `_toggle_play`、`_on_seek`、`_on_volume`、`_refresh_video_hwnd`

3. **恢复播放后重拉进度条**：`_resume_video_after_captions` 里重新拉起
   `_update_video_time_loop`，否则进度条/时间标签不再更新。

## 验证

- 「周期 `get_length()` 循环 + 后台 `stop()` + 泵事件」复现原死锁，加护栏后
  `stop()` 0.07s 返回，无卡死。
- 「播放中开字幕」完整流程：安全停 0.08s 返回、字幕启动成功、播放恢复
  `State.Playing`、收尾干净。
- 语法检查、lint 均通过。
